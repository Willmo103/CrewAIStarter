from setuptools import find_packages, setup

setup(
    # name='FirstMate',
    name="CrewCoach",
    requires=[
        "langchain_community",
        "requests",
        "crewai",
        "langchain_core",
        "ollama",
    ],
)

# TODO: Finish the setup.py file
# 1. Add the correct name for the package
# 2. Add the correct dependencies
# 3. Add the correct entry points for the package
# 4. Add the correct classifiers for the package
# 5. Add the correct license for the package
# 6. Add the correct long description for the package
